export interface CustomerData{
    name: string;
    email: string;
    phone: string;
}

export interface CustomerResponse{
    id: string;
    created_at: Date;
    email: string;
    name: string;
    phone: string;
} 